Supervised machine learning is a method where a computer learns to make predictions by studying examples that already have correct answers. The algorithm finds patterns in labeled training data, then uses those patterns to predict outcomes for new, unseen inputs. For example, it can learn to classify emails as spam or not spam by studying thousands of emails that humans have already labeled.