Supervised machine learning is a type of artificial intelligence where a model learns from labeled examples — data that already has the correct answers attached. The model studies these examples to find patterns, then uses what it learned to make predictions on new, unseen data. For instance, you might train a model on thousands of emails marked as "spam" or "not spam" so it can automatically classify future emails on its own.
